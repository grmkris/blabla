import React from "react";
import { SubgraphForm } from "../types/common";

type Props = { input: SubgraphForm; index: number };

function SubgraphRow({ input, index }: Props) {
  return <div>SubgraphRow</div>;
}

export default SubgraphRow;
